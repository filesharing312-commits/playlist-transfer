import TransferUI from '@/components/transfer-ui';

export default function Home() {
    return (
        <main className="min-h-screen bg-gray-50 py-8">
            <TransferUI />
        </main>
    );
}
